Server A의 /opt/GPT-SoVITS 경로에 제가 생성한 스크립트를 배치하고 실행해야 합니다.

file_scanner_api.py (포트 10001)
gpt-sovits-files.service (systemd 서비스)

```bash
# Server A에서 실행
pip install fastapi uvicorn
sudo cp gpt-sovits-files.service /etc/systemd/system/
sudo systemctl enable --now gpt-sovits-files
```
